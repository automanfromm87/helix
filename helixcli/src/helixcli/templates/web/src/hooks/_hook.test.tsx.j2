// PLACEHOLDER TEST — rewrite this file when you replace the
// scaffolded `{ value, set }` shape with the hook's real return.
// The current test exists so `npm test` passes immediately after
// scaffolding; it ASSERTS the placeholder, not your real logic.
import { renderHook } from '@testing-library/react'
import { describe, expect, it } from 'vitest'

import { use{{ name }} } from './use{{ name }}'

describe('use{{ name }} (placeholder)', () => {
  it('renders without throwing', () => {
    // Once you customise `use{{ name }}`'s signature, replace this
    // with assertions on the real return shape.
    expect(() => renderHook(() => use{{ name }}<unknown>(undefined as never))).not.toThrow()
  })
})
