// PLACEHOLDER TEST — rewrite when you customise `{{ name }}`'s
// markup. The default heading-name assertion only matches the
// scaffolded `<h2>{{ name }}</h2>`; once you change that, this test
// will fail and you should replace it with a real assertion about
// what the component actually does.
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'

import {{ name }} from './{{ name }}'

describe('{{ name }} (placeholder)', () => {
  it('renders its scaffolded heading', () => {
    render(<{{ name }} />)
    expect(
      screen.getByRole('heading', { name: '{{ name }}' }),
    ).toBeInTheDocument()
  })
})
